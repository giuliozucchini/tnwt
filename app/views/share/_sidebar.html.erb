	<br />

	<div class="partial-box">

		<h3 class="report-title sidetitle"><%= @report.title %></h3>
		<div>
		    <ul id='sidelist', class="report-articles">
		  		<% @articles.each do |article| %>
		  			<li><%= link_to article.title, article_path(article), 'data-report-id': @report.id %></li>
				<%end%>
			</ul>
		</div>
		
	</div>

